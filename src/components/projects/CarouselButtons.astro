---
interface Props {
  alt: string;
  index: number;
}

const { alt, index } = Astro.props;
---

<button
  type="button"
  class="w-3 h-3 rounded-full"
  aria-current="true"
  aria-label={alt}
  data-carousel-slide-to={index}></button>
